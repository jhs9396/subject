input GraphInput {
    id: Int
    label: String
    name: String
}

type GraphNode {
    id: Int
    label: String
    name: String
    radius: Int
    color: String
}

type GraphEdge {
    source: Int
    target: Int
}

type GraphOutput {
    nodes: [GraphNode]
    edges: [GraphEdge]
}